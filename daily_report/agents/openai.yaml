interface:
  display_name: "日报生成助手"
  short_description: "根据当天 Codex 对话历史和 Git 改动整理中文工作日报"
  default_prompt: "Use $daily_report to read today's Codex session history first, then combine it with Git commits or workspace changes to write a concise Chinese daily work report without modifying project code or creating commits."

policy:
  allow_implicit_invocation: true
