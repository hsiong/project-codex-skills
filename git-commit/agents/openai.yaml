interface:
  display_name: "Git Commit Assistant"
  short_description: "Safely split and create Chinese or English Git commits"
  default_prompt: "Use $git-commit to safety-check Git-known changes and create well-scoped commits in the language I request."

policy:
  allow_implicit_invocation: true
