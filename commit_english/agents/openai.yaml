interface:
  display_name: "English Commit Splitter"
  short_description: "Split Git changes into English commits by flow"
  default_prompt: "Use $commit_english to split the current Git-known changes into safe English conventional commits by functional flow and summarize the results."

policy:
  allow_implicit_invocation: true
