interface:
  display_name: "Java代码规范"
  short_description: "按现有项目风格生成最小改动的 Java 代码"
  default_prompt: "Use $java-code-style to generate or revise Java backend code with project-consistent layering, validation, comments, and minimal structural change."

policy:
  allow_implicit_invocation: true
