interface:
  display_name: "GitHub Issue Writer"
  short_description: "Check duplicates and draft English GitHub issues"
  default_prompt: "Use $github-issue-generator to search for similar issues first, then read the repository issue template and draft a non-duplicate English GitHub issue in Markdown."

policy:
  allow_implicit_invocation: true
